# ביקורת SEO - mikra.shop

נבדק 2026-09-15. sitemap: 30 כתובות. robots: `User-Agent: *\nAllow: /\n\nSitemap: https://mikra.shop/sitemap.xml\n\n(200 text/`

## ממצאים

| חומרה | איפה | הבעיה | התיקון |
|---|---|---|---|
| must | app/products/[slug]/page.tsx generateMetadata (line 24), lib/schema.ts productSchema name (line 113), components/ProductView.tsx H1 - live pairs /products/beseter + /products/beseter-gold, /products/avot + /products/avot-black, /products/libi-er + /products/libi-er-gold | Three finish-variant pairs (6 of the 15 product URLs) share an identical <title> ('בְּסֵתֶר · מִקְרָא'), identical og:title, identical H1 and identical Product.name in JSON-LD - the catalog entries have the same `name`/`nameLatin` and differ only by `finish`. Google sees duplicate titles across distinct canonical URLs and may cluster them or rewrite the titles. The Google feed (app/feed.xml/route.ts lines 57-73) already disambiguates siblings with the finish label; the pages do not. | In app/products/[slug]/page.tsx generateMetadata, compute `const siblings = PRODUCTS.filter(x => x.name === p.name)` and set `title: siblings.length > 1 ? `${p.name} · ${FINISHES[p.finish]}` : p.name` (FINISHES from lib/catalog gives 'זהב'/'שחור'/'כסף'); apply the same expression to openGraph.title. In lib/schema.ts productSchema set `name` the same way (or keep name and add `color` - already present - plus a distinct `alternateName`). Reuse the sibling helper that app/feed.xml/route.ts already has at line 57 rather than duplicating it. Optionally show the finish as a subtitle under the H1 in components/ProductView.tsx. |
| must | app/products/[slug]/page.tsx line 25 (`description: `${p.short}. ${p.story.slice(0, 120)}``) and app/blessings/[slug]/page.tsx line 25 | Product meta descriptions run past 155 chars and are cut mid-word: /products/toldot is 169 chars and ends '...השבב, בתוך מסג' (the story is sliced at 120 chars blindly, then appended to a 47-char `short`). /blessings/eshet-chayil is 156 chars (over by one). Google truncates at roughly 155 chars, so the visible snippet ends in a broken word. | Add a helper in lib/seo.ts (or lib/text.ts) `clampWords(s: string, max = 150)` that trims at the last space before `max` and appends '…' only if trimmed. In app/products/[slug]/page.tsx use `description: clampWords(`${p.short}. ${p.story}`)`. In app/blessings/[slug]/page.tsx wrap the template the same way or drop the trailing `${b.words} מילים שנצרבות על שבב אחד.` clause when the result exceeds 150. |
| must | app/sitemap.ts line 11 (`lastModified: new Date()`) | Every one of the 30 sitemap URLs carries the same request-time timestamp (2026-09-15T10:21:28.486Z on this crawl) - the file is regenerated with 'now' on each fetch. Google treats sitemaps whose lastmod is always current as unreliable and ignores lastmod for the whole file, so the sitemap gives no recrawl signal at all. (`priority` is likewise ignored by Google; harmless.) The in-progress guides entries in the working tree already use a real `g.published` date, which is the right pattern. | Either delete `lastModified` from the `url()` helper in app/sitemap.ts, or add an `updated: 'YYYY-MM-DD'` field to Product in lib/catalog.ts and to Blessing in lib/blessings.ts and pass `lastModified: new Date(p.updated)` per entry; for static pages (home, /blessings, /craft, legal) keep a single hand-maintained constant `SITE_UPDATED` in lib/site.ts. Do not derive it from build time (`new Date()` at module scope has the same problem after every deploy). |
| should | app/blessings/page.tsx metadata (lines 6-11) and app/craft/page.tsx metadata (lines 11-16) - live /blessings and /craft | Neither page defines `openGraph`, so they inherit the root layout's object wholesale: og:url = https://mikra.shop, og:title = 'מִקְרָא · כל הכתוב. בגודל של גרגר.', og:image = hero. A WhatsApp/Facebook share of /blessings or /craft previews as the homepage, and Facebook attributes the share to the home URL because og:url is the URL it treats as canonical. | Add to app/blessings/page.tsx metadata: `openGraph: { type: 'website', url: '/blessings', title: 'חמש הברכות · מִקְרָא', description: <same as meta description>, images: [{ url: '/scene/pair-trio.jpg', alt: 'חמש הברכות' }] }` and the same shape for app/craft/page.tsx with url '/craft' and a craft/chip image. Mirror what app/categories/[slug]/page.tsx lines 42-49 already do. |
| should | app/blessings/[slug]/page.tsx generateMetadata (lines 23-42) - live /blessings/eshet-chayil | The page sets openGraph but not `twitter`, so twitter:title is the homepage title and twitter:image is the hero while og:image is the blessing's product photo. Also no BreadcrumbList JSON-LD even though a visible breadcrumb (בית / הברכות / אשת חיל) is rendered at lines 63-69; /blessings and /craft have no breadcrumb schema either. | In app/blessings/[slug]/page.tsx: hoist the photo lookup into a `const photo = blessingPhotos(...)[b.id] ?? '/hero/hero-landscape.jpg'` and add `twitter: { card: 'summary_large_image', images: [photo] }` (same as app/products/[slug]/page.tsx line 36). In the page body add `<JsonLd data={[breadcrumbSchema([{ name: 'מִקְרָא', path: '/' }, { name: 'הברכות', path: '/blessings' }, { name: b.plain, path: `/blessings/${b.id}` }])]} />` importing JsonLd and breadcrumbSchema from lib/schema.ts. Add a two-item breadcrumb to app/blessings/page.tsx and app/craft/page.tsx as well. |
| should | app/layout.tsx lines 53 and 58 (og/twitter image), public/hero/hero-landscape.jpg; app/products/[slug]/page.tsx lines 34-36 (product og:image = .webp) | The declared og:image dimensions (2400x1357) do not match the file (3000x1191, ratio 2.52:1), and the JPEG is 311,826 bytes - over the ~300 KB ceiling at which WhatsApp silently drops the preview image (the layout comment explicitly says WhatsApp sharing is the priority in Israel). Product pages use the 385 KB product .webp as og:image; WhatsApp and LinkedIn do not reliably render WebP link previews, so product shares can arrive image-less. | Export public/og/default.jpg at 1200x630, quality ~80, under 200 KB, and set in app/layout.tsx `images: [{ url: '/og/default.jpg', width: 1200, height: 630, alt: 'מִקְרָא' }]` and `twitter.images: ['/og/default.jpg']`. For products, in app/products/[slug]/page.tsx use `const og = p.scenes?.[0] ?? p.image` (scenes are JPEGs, e.g. /scene/toldot-wood.jpg) for both openGraph.images and twitter.images, or add app/products/[slug]/opengraph-image.tsx (Next ImageResponse) to emit a 1200x630 PNG per product. Keep each under 300 KB. |
| should | lib/schema.ts organizationSchema line 45 (`logo: abs('/hero/hero-landscape.jpg')`) and the in-progress articleSchema image | Organization.logo points at the 3000x1191 lifestyle hero photograph, not the brand mark. Google's logo guidelines (used for the knowledge panel and merchant listings) expect the actual logo, at least 112x112 and ideally square; a wide product photo is unlikely to be accepted as a logo. | Rasterise app/icon.svg to public/logo-512.png (512x512, brand background), then in lib/schema.ts organizationSchema set `logo: { '@type': 'ImageObject', url: abs('/logo-512.png'), width: 512, height: 512 }` and optionally keep the hero as `image: abs('/hero/hero-landscape.jpg')`. Point the WIP articleSchema `image` at a real article image rather than the hero as well. |
| should | lib/schema.ts organizationSchema (lines 37-50) and the stale comment at lines 17-18 | The comment says no phone/email is public so contactPoint is omitted - but lib/company.ts now holds phone 050-775-5705, email mikrajewelry@gmail.com and the Modiin address, and components/Footer.tsx lines 56-73 render all three on every live page. Organization schema therefore lacks telephone/email/address/contactPoint that the visible site already publishes (these feed Google's merchant trust signals). No `sameAs` either - the site links to no social profiles anywhere, so do not invent them. | In lib/schema.ts import COMPANY from lib/company.ts and add to organizationSchema: `telephone: COMPANY.phone, email: COMPANY.email, address: { '@type': 'PostalAddress', streetAddress: 'גולדה מאיר 112', addressLocality: 'מודיעין', addressCountry: 'IL' }, contactPoint: [{ '@type': 'ContactPoint', contactType: 'customer service', telephone: COMPANY.phone, email: COMPANY.email, availableLanguage: 'he' }]`. Update the lines 17-18 comment. Add `sameAs: [...]` only when Instagram/Facebook/TikTok profiles exist and are linked from the footer. |
| should | app/layout.tsx title template (line 35), app/products/[slug]/page.tsx line 24, app/categories/[slug]/page.tsx line 39, lib/catalog.ts CATEGORIES.title | Every non-home <title> is 16-20 characters: bare name + brand ('שרשראות · מִקְרָא', 'תּוֹלְדוֹת · מִקְרָא'). That leaves the strongest ranking slot without the actual query terms (שרשרת עם ברכה, תליון עץ החיים כסף 925, צמיד לגבר עם ברכה...). Titles also carry niqqud (מִקְרָא, תּוֹלְדוֹת, בְּסֵתֶר) while searchers type unpointed text; lib/brand.ts already defines `BRAND.plain` 'בלי ניקוד — למטא־דאטה' and app/feed.xml/route.ts line 27 has a `plain()` stripper, yet the title template hardcodes the pointed form. | In app/layout.tsx use `template: `%s · ${BRAND.plain}`` (import BRAND). Move the niqqud stripper from app/feed.xml/route.ts to lib/hebrew.ts as `stripNiqqud()` and use it in titles. Add `seoTitle` to each CATEGORIES entry in lib/catalog.ts (e.g. necklaces: 'שרשראות עם ברכה צרובה על שבב', bracelets: 'צמידים עם ברכה צרובה על שבב') and use it in app/categories/[slug]/page.tsx `title`. For products build `title: `${stripNiqqud(p.name)} · ${CATEGORIES[p.category].singular ?? CATEGORIES[p.category].title} ${MATERIALS[p.material].label}`` (e.g. 'תולדות · שרשרת עץ החיים כסף 925 · מקרא'); keep pointed names in the H1 and og:title if the brand wants them. Target 35-55 characters. |
| should | components/ProductStory.tsx line 161 (`alt=""` on scene photos), components/BlessingCard.tsx line 168, components/SectionPhoto.tsx line 38, app/categories/[slug]/page.tsx lines 181/210 | No <img> lacks an alt attribute, but many meaningful photos carry alt="": /products/toldot 19 of 25, /products/beseter 16 of 20, /blessings 27 of 27, /categories/necklaces 9 of 17. Gallery thumbnails inside aria-labelled buttons are correctly decorative, but the ProductStory scene photos (/scene/toldot-wood.jpg etc.) are the same files listed in Product.image JSON-LD and the only server-rendered copies on the page - Google Images gets no text for them. lib/worn.ts already shows the right pattern (per-photo alt strings). | Change `scenes: string[]` in lib/catalog.ts Product to `scenes: { src: string; alt: string }[]` (or add a SCENE_ALT record keyed by src, like lib/worn.ts) with alts such as 'תליון עץ החיים בכסף 925 על רקע עץ, השבב הכחול במרכז'; pass `alt={photo.alt}` in components/ProductStory.tsx line 161 and in the scene branch of components/ProductView.tsx. In components/BlessingCard.tsx line 168 use `alt={p.name}`. Keep alt="" on the hover-swap duplicate in ProductCard.tsx line 90 and on SectionPhoto backgrounds (decorative). |
| should | app/robots.ts lines 15-18 | robots.txt is a bare Allow: /. /checkout and /checkout/success carry `robots: { index: false }` meta (good), but /api/* routes (admin revalidate, checkout endpoints) are crawlable and Googlebot will probe them from script references, wasting crawl on a 30-page site and risking error-page indexing. | In app/robots.ts return `rules: [{ userAgent: '*', allow: '/', disallow: ['/api/', '/checkout'] }]` in the live branch. Keep the sitemap line. |
| nice | lib/schema.ts productSchema line 133 (`availability: 'https://schema.org/InStock'` hardcoded) and lib/catalog.ts Product type | Product schema is otherwise complete for Google merchant listings (offers, price, priceCurrency ILS, availability, itemCondition, brand, sku, image[], material, color, shippingDetails, hasMerchantReturnPolicy) and correctly has no aggregateRating/review - keep it that way. But availability is a constant with no stock flag anywhere in lib/catalog.ts, so a sold-out design would still advertise InStock in search; the feed also emits g:mpn = sku while the schema has no mpn. | Add `inStock?: boolean` (default true) to Product in lib/catalog.ts; in lib/schema.ts productSchema set `availability: product.inStock === false ? 'https://schema.org/OutOfStock' : 'https://schema.org/InStock'` and add `mpn: product.sku` and `url: `${SITE_URL}/products/${product.slug}`` on the Product node. Mirror the flag in app/feed.xml/route.ts g:availability. |
| nice | app/craft/page.tsx (FAQ const, rendered via components/Accordion) and lib/faq.ts productFaq | FAQPage JSON-LD is present and valid on product pages (9 Q/A), but the identical 9 questions are emitted on all 15 product pages, and /craft renders a visible 6-question FAQ with no FAQPage schema. Note Google stopped showing FAQ rich results for non-government/non-health sites in August 2023, so FAQPage is harmless but not a ranking lever - do not invest in it beyond consistency. | Optional: in app/craft/page.tsx add `<JsonLd data={[faqSchema(FAQ)]} />` (import from lib/schema.ts) so the visible FAQ and markup match. Leave product FAQPage as is. |
| nice | app/sitemap.ts line 16 (`url('', 1)`), app/layout.tsx `alternates.canonical: '/'`, lib/schema.ts breadcrumb item for '/' | The homepage is referenced in two forms: canonical and sitemap say 'https://mikra.shop' (no trailing slash) while BreadcrumbList items say 'https://mikra.shop/'. Google normalises a bare origin to the slash form, so this is cosmetic, but one form is cleaner for URL-inspection and log matching. | In app/sitemap.ts change `url('', 1)` to `url('/', 1)` so the sitemap emits 'https://mikra.shop/'; Next resolves the '/' canonical to the same. No other change needed. |
| nice | app/categories/[slug]/page.tsx lines 129-130, app/blessings/[slug]/page.tsx line 152, lib/faq.ts line 50 | Indexable copy contradicts itself: the category page says every model comes with each of the 5 blessings ('כל דגם עם כל אחת מ־5 הברכות'), the blessing page says every model in the catalog can carry this blessing, but the product FAQ (also in FAQPage JSON-LD) says 'איך בוחרים בין 3 הנוסחים?' because toldot's `blessings` array holds only 3 of the 5. Search snippets and the FAQ markup expose the mismatch. | Either make every product's `blessings` list all five ids in lib/catalog.ts, or change the category line to `כל דגם עם ${'עד'} ${BLESSINGS.length} ברכות לבחירה` and the blessing page copy to reference `carriers.length` models ('{n} דגמים בקטלוג נושאים את {b.plain}'). |
| nice | app/layout.tsx line 39 (`keywords`), app/products/[slug]/page.tsx openGraph.type | `keywords` meta is ignored by every major engine (harmless). Product pages use og:type 'website'; Facebook/Pinterest can show price in share cards when product price tags are present. | Delete the `keywords` array in app/layout.tsx. Optionally add to the product generateMetadata return `other: { 'product:price:amount': String(p.price), 'product:price:currency': 'ILS' }`. |
| nice | Working tree: app/guides/, lib/guides.ts, lib/guides.data.ts (untracked), app/sitemap.ts / app/layout.tsx / lib/schema.ts / components/Footer.tsx (modified) - live /guides returns 404 | Another session is mid-way through adding gift guides: the footer link to /guides, the sitemap entries and articleSchema are uncommitted while the live site 404s on /guides. Not a live defect today, but if the Footer change ships before the pages, every page will link to a 404. The guides sitemap entries use real `g.published` dates - the right pattern for the rest of the sitemap (see the lastModified finding). hreflang is correctly absent: the site is Hebrew-only with lang=he, og:locale he_IL and WebSite.inLanguage he-IL all consistent - do not add hreflang. | Commit app/guides, lib/guides*.ts, the sitemap and footer edits together in one commit. In lib/schema.ts articleSchema, use a per-guide image (add `image` to the guide data) instead of '/hero/hero-landscape.jpg', and consider `dateModified` separate from `datePublished`. |

## העמודים

| עמוד | title | תווים | description | תווים | h1 | schema | img בלי alt |
|---|---|---|---|---|---|---|---|
| https://mikra.shop/ | מִקְרָא · תכשיטי ננו - ברכה שלמה על שבב אחד | 43 | שרשראות וצמידים בכסף 925 ובפלדת אל־חלד, ובליבת כל אחד מהם שב… | 137 | כל הנוסח. לא שורה ממנו. | Organization, WebSite | 0 |
| https://mikra.shop/categories/necklaces | שרשראות · מִקְרָא | 17 | התליון נח על עצם הבריח, קרוב מספיק כדי להרגיש אותו. כל שרשרת… | 129 | שרשראות | Organization, WebSite, ItemList, BreadcrumbList | 0 |
| https://mikra.shop/categories/bracelets | צמידים · מִקְרָא | 16 | על פרק היד השבב נמצא בשדה הראייה שלכם כל היום. הצמידים בפרופ… | 118 | צמידים | Organization, WebSite, ItemList, BreadcrumbList | 0 |
| https://mikra.shop/products/toldot | תּוֹלְדוֹת · מִקְרָא | 20 | מדליון עץ החיים בכסף 925, השבב משובץ בין הענפים. הדגם הגדול … | 169 | תּוֹלְדוֹת | Organization, WebSite, Product, FAQPage, BreadcrumbList | 0 |
| https://mikra.shop/products/beseter | בְּסֵתֶר · מִקְרָא | 18 | מגן דוד בקווים חדים, השבב במרכז המשושה. מגן דוד בגזרה גיאומט… | 141 | בְּסֵתֶר | Organization, WebSite, Product, FAQPage, BreadcrumbList | 0 |
| https://mikra.shop/blessings | חמש הברכות · מִקְרָא | 20 | חמישה נוסחים שנצרבים על השבב: ברכת התינוק, ברכת הפרנסה, הברכ… | 116 | חמש ברכות. אחת שלכם. | Organization, WebSite | 0 |
| https://mikra.shop/blessings/eshet-chayil | אשת חיל · מִקְרָא | 17 | עשרים ושניים הפסוקים של משלי ל״א, אות אחר אות לפי סדר האלף־ב… | 156 | אֵשֶׁת חַיִל | Organization, WebSite | 0 |
| https://mikra.shop/craft | הטכנולוגיה · מִקְרָא | 20 | כיצד נצרבת ברכה שלמה על שטח של חצי מילימטר: ליתוגרפיית קרן י… | 110 | איך מכניסים ברכה לתוך גרגר? | Organization, WebSite | 0 |
