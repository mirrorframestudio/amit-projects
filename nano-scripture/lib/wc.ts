import 'server-only';

/**
 * העטיפה שאוסרת שימוש מהלקוח.
 *
 * התעבורה עצמה יושבת ב-wcClient, כי סקריפט של שורת פקודה חייב לגשת
 * אליה ו-server-only נכשל מחוץ ל-Next. ההפרדה היא בדיוק בין השניים:
 * קוד האפליקציה מייבא מכאן ומקבל את ההגנה, הסקריפט מייבא משם.
 */
export * from './wcClient';
